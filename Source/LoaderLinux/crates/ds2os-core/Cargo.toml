[package]
name = "ds2os-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Steam, Proton and configuration logic for the ds2os Linux loader"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
sha2 = "0.10"

# Steam's location on Windows is a registry value, not a known path.
[target.'cfg(windows)'.dependencies]
winreg = "0.55"
